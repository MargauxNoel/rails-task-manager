<% @tasks.each do |task| %>
  <h2> <%= link_to task.title, task_path(task) %> </h2>
  <input type="checkbox" value= <%= "task.completed" %> >
  <p> <%= link_to 'Destroy this task', delete_path(task), method: :delete %> </p>
<% end %>

<h4><%= link_to "Add a task", tasks_new_path %></h4>
